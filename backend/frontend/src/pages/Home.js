import React from 'react';
import Navbar from '../components/Navbar/Navbar';

const Home = (props) => {

    return(
        <React.Fragment>
            <Navbar></Navbar>
        </React.Fragment>
    )
}

export default Home;
